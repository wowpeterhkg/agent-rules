# Evidence log

One line per event. Anyone can append. Read at each quarterly review.

A core rule with **zero saves and any false positives** is deleted.
A core rule with **zero of both** is dead weight and is also deleted.

## Saves

| Date | Rule | What it prevented |
| --- | --- | --- |

## False positives

| Date | Rule | What it wrongly blocked or nagged about |
| --- | --- | --- |

## Origin

| Rule | Why it exists |
| --- | --- |
| `10-git-ceiling` | Promoted from `ppt/CLAUDE.md`, where it was written after agents merged/pushed without asking. |
| `50-working-agreement` | Migrated from a personal `agent.md` refined over many sessions. |
