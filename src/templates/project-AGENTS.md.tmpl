# @PROJECT@ — agent instructions

Global house rules (git ceiling, safety, secrets, working agreement) are installed on the
machine and apply here. This file holds only what is true of **this** repo.

Detailed stack rules for this project live in `.agent/rules/`. Read the one that matches what
you are about to touch.

## Stack

<!-- Languages, frameworks, versions. Keep it short; detail belongs in .agent/context/tech-stack.md -->

## Shape

<!-- Services/packages and what each owns. Name the boundaries that must not be crossed. -->

## Build, test, run

<!-- The exact commands. An agent should never have to guess these. -->

## Things that will bite you

<!-- Non-obvious traps: a service that must start first, a migration that is not reversible,
     a test suite that needs a container, a directory that looks dead but is not. -->

## Overrides

<!-- Project rules load after user rules and win on conflict. State any override explicitly,
     e.g. "Unlike the house default, this repo squash-merges." -->
